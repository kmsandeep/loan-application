server:
  port: 8082
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    user: sa
    password:
  kafka:
    bootstrap-servers: localhost:9092

  h2:
    console:
      enabled: true